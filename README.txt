Voxel.js minigame

Needs to be in htdocs in XAMPP folder

to run cd to the correct directory and type

npm install to install all the dependencies

then type browserify index.js -o bundle.js

and finally in a browser type localhost/htdocs/minigame/index.html